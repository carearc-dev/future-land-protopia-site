# Codex運用ルール

## 作業前

- このフォルダに `CLAUDE.md` がある場合は、作業前に全文を読み、プロジェクト固有の仕様・禁止事項・本番構成として扱う。
- `CLAUDE.md` にClaude専用の機能やパスが書かれている場合は、Codexで利用可能な方法へ読み替え、利用できると勝手に判断しない。
- 最初に現在のブランチと `git status` を確認し、既存の変更を上書きしない。
- 変更が本番公開へ影響するか分からない場合は、作業を止めて亀井さんに確認する。

## 変更と確認

- 依頼された範囲だけを最小限変更する。
- リポジトリに確認方法やテストがある場合は実行し、結果を報告する。
- 時限情報、金額、制度、法律、外部サービスの仕様などは、必要に応じて最新の公式情報を確認する。

## GitHub送信と本番公開

- 初期状態では、ローカルでの変更と確認までで止める。明示的な承認なしにcommit、push、GitHubの確認ページ作成、本番公開をしない。
- 修正依頼を受けたこと自体を、GitHub送信や本番公開の承認とみなさない。
- mainへ直接pushしない。
- GitHubへ送る前に、変更内容・変更ファイル・確認結果・本番への影響を、専門用語を避けた日本語で報告し、亀井さんの承認を待つ。
- 承認後は作業用ブランチへcommit・pushし、GitHubの確認ページを作る。本番公開はしない。
- GitHubの確認ページを本番へ取り込む操作は行わない。最終操作は亀井さんが行う。
- GitHub Actionsの手動実行、FTP、FTPS、SFTP、その他の直接デプロイは、通常のGitHub送信とは別に明示的な承認がある場合だけ行う。

## 承認前の報告

- 次の5点を短く報告する。
  1. 何を変えたか
  2. どのページ・機能に影響するか
  3. どのように確認したか
  4. GitHubへ送っただけで本番が変わるか
  5. 亀井さんが次に行う操作と、その操作の意味

## 認証情報

- パスワード、APIキー、アクセストークン、Secrets、`.env` の内容を表示・コピー・コミットしない。
- 認証情報らしき値を見つけた場合は値を出力せず、場所だけを報告する。

## コミットメッセージ

- 日本語で「何を・なぜ変更したか」が分かる内容にする。

